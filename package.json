{
  "name": "postloom",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @postloom/database db:generate",
    "db:push": "pnpm --filter @postloom/database db:push",
    "db:seed": "pnpm --filter @postloom/database db:seed",
    "db:studio": "pnpm --filter @postloom/database db:studio",
    "db:setup-roles": "pnpm --filter @postloom/database db:setup-roles",
    "worker:dev": "pnpm --filter @postloom/worker dev",
    "worker:test-enqueue": "pnpm --filter @postloom/worker test:enqueue",
    "blog:dev": "pnpm --filter @postloom/blog dev",
    "admin:dev": "pnpm --filter @postloom/admin dev"
  },
  "devDependencies": {
    "turbo": "^2.8.12",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "prisma",
      "@prisma/engines",
      "esbuild",
      "sharp"
    ]
  }
}
